# Generate with: node scripts/generate-vapid.mjs (optional — for Web Push backend)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# Optional Postgres for persisting push subscriptions
DATABASE_URL=
