# Generate with: node scripts/generate-vapid.mjs
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@crashgames.demo

# Neon Postgres connection string (Vercel Marketplace → Neon)
DATABASE_URL=

# Secret for /admin/notifications (send to all subscribers)
ADMIN_PUSH_SECRET=
